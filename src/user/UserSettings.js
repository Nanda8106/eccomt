import React from 'react';
import Base from '../core/Base';

const UserSettings = () => {
    return (
        <Base>
            <div className="under-construction">
                <h3>User Settings is in Underconstruction</h3>
            </div> 
        </Base>
    );
}

export default UserSettings;